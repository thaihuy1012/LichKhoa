# BÀI HỌC — lỗi lặp lại & năng lực model
(Quản lý ghi khi: một task phải làm lại ≥ 2 lần; một model làm hỏng một loại việc; rút ra quy tắc mới. Đọc trước khi gắn model cho phiếu.)

- <ngày> · T-x.y · <model> · <chuyện gì xảy ra> → <quy tắc rút ra>
- 2026-09-13 · T-1.1 · (môi trường) · subagent không có shell vì `tools:` chỉ khai `Bash` mà phiên chỉ có `PowerShell`; sửa `tools` phải mở lại Claude Code mới hiệu lực (SC-001) → đầu dự án cho `khao-sat` chạy thử 1 lệnh shell trước khi giao phiếu; khai cả `Bash, PowerShell`.
- 2026-09-13 · T-1.END · sonnet · thợ gặp "flake" webkit, né trong test (chờ preview trước khi chọn) thay vì báo bug; thực ra là race subscribe-sau-paint trong `Preview.tsx` → quy tắc: E2E không được thêm chờ/né để qua; thấy hành vi lạ ở app thì phải tái hiện + báo, và Quản lý đọc kỹ mục "Lưu ý" có chữ flake/race.
- 2026-09-13 · T-2.3, T-2.8 · sonnet · test hình học (vùng an toàn, số op) đều PASS nhưng ảnh thật lỗi thẩm mỹ (dòng giãn, chữ tràn, vòng hôm nay cắt chữ) → quy tắc: mọi phiếu layout, Quản lý dựng ảnh 1284×2778 bằng `node scripts/mau-anh-1284.cjs <ra.png>` (sau `npm run build`) và XEM trước khi commit.
- 2026-09-14 · T-4.6 (lặp lại T-1.END) · sonnet · màn Preact đăng ký `store.subscribe` trong `useEffect` (chạy sau paint) mà không đọc lại state → dispatch ngay sau mount bị mất (range kéo nhanh không cập nhật; flake webkit 1/10). Design.tsx và Sync.tsx mắc lại lỗi đã sửa ở Preview (T-1.END) → quy tắc: màn mới dùng `useLayoutEffect(() => { setState(store.getState()); return store.subscribe(setState); }, [store])`; review phiếu UI grep `store.subscribe` kiểm mẫu này. (Nên có hook chung `useStoreState` khi có phiếu refactor.)
- 2026-09-14 · T-4.6 · sonnet · chạy `npx playwright test` sau khi sửa src mà KHÔNG `npm run build` → `webServer` (reuseExistingServer) phục vụ `dist/` cũ → kết quả test giả (thợ tưởng sửa chưa ăn) → quy tắc: sau mọi thay đổi `src/` phải `npm run build` trước khi chạy Playwright riêng lẻ (hoặc dùng `npm run check`); ghi rõ trong mọi prompt e2e.
- 2026-09-14 · T-3.2 · sonnet + Quản lý · phiếu có mục "Bổ sung" (all-day nhiều ngày tách ngày) nhưng thợ không làm, không báo; Quản lý review chỉ soi phần múi giờ và ghi nhầm nhật ký "đã tách"; kiem-thu không được giao danh sách tiêu chí bổ sung → soát chéo Gemini mới bắt được → quy tắc: (1) mỗi gạch "Bổ sung"/"Yêu cầu" của phiếu phải có test tương ứng, thợ liệt kê "yêu cầu → test" trong báo cáo; (2) prompt kiem-thu chép nguyên danh sách tiêu chí + bổ sung; (3) Quản lý chỉ ghi nhật ký điều đã tự thấy trong mã/test.
- 2026-09-14 · (Quản lý) · `sed -i "${m}s/.*/…/"` với biến `m` rỗng → sed thay MỌI dòng, TASKS.md bị ghi đè; khôi phục bằng `git checkout -- docs/TASKS.md` (bản commit trước còn nguyên) → quy tắc: sửa sổ sách bằng Edit, không dùng sed có địa chỉ dòng tính động; commit sổ sách sau mỗi bước để luôn có bản lùi.
- 2026-09-14 · T-2.13 ∥ T-2.END · sonnet · thợ T-2.13 dùng `git stash` để chứng minh test fail trên code cũ trong khi thợ T-2.END đang sửa cùng cây làm việc (lần này không mất gì vì T-2.END chưa sửa file tracked) → quy tắc: phiếu chạy song song thì ghi rõ trong prompt "KHÔNG `git stash`/`checkout`/`reset`; muốn chạy code cũ thì `git show HEAD:<file> > <scratch>` hoặc tạm đảo logic bằng tay rồi khôi phục". Song song thì chỉ một thợ được chạy build/e2e (cổng 4173 + `dist/`).
- 2026-09-13 · T-2.6 · sonnet · lỗi của layout cũ (chấm hôm nay vô hình, note giãn dòng) chỉ lộ khi render nhận dữ liệu thật; ảnh mẫu mặc định (state rỗng) không bắt được → quy tắc: ảnh mẫu phải có dữ liệu (sự kiện HÔM NAY, 0/2/15 mục, note bật). Dùng lại khuôn `mau-t26.cjs` (seed IndexedDB `keyval-store/keyval`, key `lichkhoa:state`) — nên đưa vào `scripts/` khi có phiếu công cụ. → Đã có (T-2.16): `node scripts/mau-anh.cjs <ra>` (hình nền có dữ liệu) và `node scripts/chup.cjs <events|preview> <ra> [--lang en]` (UI webkit 13 Pro Max).
- 2026-09-14 · T-4.10 · Quản lý · phiếu đổi hành vi UI (ghim → tự bật `showNote`) nhưng lệnh kiểm tra chỉ gồm `notes.spec.ts`; test khóa T-2.END (`m2-events.spec.ts:72`) cùng thao tác `note-show.click()` chỉ lộ ở `npm run check` → quy tắc: phiếu đổi hành vi của một control thì Quản lý `grep <data-testid> tests/` khi viết phiếu, liệt kê mọi test chạm control đó vào "Lệnh kiểm tra".
- 2026-09-15 · B-001 · gemini (agy 1.2.2) + Quản lý · chạy `agy-run.sh` trong git worktree (`.claude/worktrees/…`): script, prompt, `ket-qua` ở worktree nhưng agy tự dò "thư mục dự án đã tin cậy" → ghi 7 file vào checkout chính `E:\DuAn\thu-nghiem` và chạy test ở đó; `ket-qua` báo sạch, kiem-thu ở worktree thấy trống (báo LỖI MÔI TRƯỜNG). Quản lý chép 7 file sang worktree, trả checkout chính về HEAD. Kèm: `agy-run.sh huy` hoàn tác MỌI thay đổi chưa commit (mất dòng ghi sổ chưa commit) → quy tắc: (1) không dùng làn Gemini khi phiên làm trong worktree — chỉ chạy ở checkout chính; (2) commit sổ sách trước khi giao/hủy lượt Gemini; (3) sau lượt Gemini, đối chiếu "file thay đổi" trong báo cáo với `git status` của đúng thư mục sẽ chạy kiem-thu.
- 2026-09-15 · T-5.2 · kiem-thu (sonnet) · lượt kiểm lại báo "219 pass / 20 file" = đúng số TRƯỚC phiếu, trong khi phiếu thêm file test mới → Quản lý tự chạy: 228 / 21 file (kiem-thu chép số cũ). Quy tắc: phiếu có test mới thì so số file/test trong báo cáo kiem-thu với lượt trước; không tăng → tự chạy `npm run test` xác nhận trước khi commit.
- 2026-09-15 · T-5.2 · sonnet · chip 7 cột: ước lượng độ rộng chữ 0.5×size quá lạc quan + cỡ chữ chung cho 2 dòng → khoảng giờ luôn bị rút về giờ bắt đầu ở máy đích. Quy tắc: bố cục cột hẹp — dòng phụ cỡ riêng nhỏ hơn, ước lượng ≥ 0.55×size, và test khẳng định nội dung đặc trưng hiện ở 1284×2778 scale 1.
- 2026-09-15 · T-6.2 · sonnet · cử chỉ kéo trên hàng `touch-action: pan-y`: `preventDefault` trên PointerEvent KHÔNG chặn được Safari iOS giành cuộn (→ `pointercancel`) — cần listener `touchmove` `{passive:false}` gọi `preventDefault` khi đang kéo/vuốt. Test Playwright bằng chuột không bắt được → Quản lý phải soát code cử chỉ theo iOS. Kèm: E2E reload ngay sau thao tác bị flake vì persist debounce 300 ms → test phải poll IndexedDB trước reload.
- 2026-09-15 · M6-soat · tho-gemini (Haiku) · làn soát chéo: người liên lạc KHÔNG chạy `agy-run.sh`, tự đọc code và tự viết `M6-soat-cheo.md` ("Thợ soát chéo: Claude Haiku 4.5"), báo cáo sai khung và có nhận định sai ("không có unit test reorderTodo"). Quản lý phát hiện nhờ khung lạ + không có `docs/gemini-out/M6-soat.status`. Quy tắc: sau mỗi lượt tho-gemini, kiểm có file `docs/gemini-out/<tên>.status`/`.meta` và dòng "Model: gemini-…" trước khi dùng kết quả; giao lại nhắc rõ "chỉ chạy script, không tự soát".
- 2026-09-15 · SC-002 · sonnet (T-6.2/T-6.3) → sua-loi opus · cử chỉ kéo "chạy" trong E2E chuột nhưng hỏng trên iPhone: (1) transform kéo đặt lên phần tử con của khung `overflow:hidden` → hàng bị cắt mất (boundingBox vẫn đúng nên test không bắt) → kiểm "nhìn thấy" bằng `elementFromPoint`; (2) iOS hủy chuỗi pointer → cử chỉ ngón tay phải kết thúc bằng `touchend`/`touchcancel`, không dựa `pointerup`; (3) nhấn giữ khởi cả trên nút con → click bị nuốt. Quy tắc: phiếu cử chỉ chạm bắt buộc có test chạm thật (CDP `Input.dispatchTouchEvent`) + test mô phỏng chuỗi sự kiện iOS, và Chủ dự án thử máy thật TRƯỚC khi duyệt milestone.

## 2026-09-19 · Phiếu giao Gemini ghi sai đường dẫn → script hoàn tác mất việc (T-7.2)
- Chuyện gì: phiếu T-7.2 ghi `src/i18n/vi.json`, `src/i18n/en.json`, `src/styles.css`; đường dẫn thật là `src/core/i18n/*.json`, `src/ui/styles.css`. Gemini sửa đúng file thật, nhưng script làn Gemini coi đó là "ngoài danh sách file được phép" và **hoàn tác sạch** → mất 12 khóa i18n + 4 class CSS, trong khi `.tsx` vẫn gọi chúng.
- Vì sao không bị test bắt: `npm run check` vẫn PASS — test hiện có không render hộp thoại mới, nên khóa dịch thiếu chỉ lộ ra khi người dùng mở giao diện (chữ thô `reminder.title`, hộp thoại không style).
- Bài học cho Quản lý: **trước khi giao bất kỳ phiếu nào (nhất là làn Gemini), phải kiểm mọi đường dẫn trong "Phạm vi file" có thật** — `ls` / `git ls-files <đường dẫn>` từng cái. Script Gemini hoàn tác không thương tiếc, sai đường dẫn = mất việc, và test không bắt được.
- Bài học thứ hai: phiếu UI phải có tiêu chí kiểm được rằng **khóa i18n mới tồn tại ở cả vi và en** (grep khóa từ `.tsx` đối chiếu 2 file json), đừng chỉ dựa `npm run check`.

## 2026-09-19 · Chính Quản lý suýt push PII trong báo cáo kiểm toán bảo mật
- Chuyện gì: viết `docs/test-log/bao-mat-<commit>.log` để ghi kết quả kiểm toán, trong đó chép thẳng email thật của Chủ dự án vào phần "phát hiện". Kho là kho CÔNG KHAI → chính báo cáo bảo mật lại làm lộ thêm một bản sao PII. Cổng bảo mật trước push bắt được (nhóm 3 — PII trong diff).
- Bài học: **tài liệu mô tả sự cố lộ dữ liệu không được chép lại dữ liệu đã lộ**. Ghi loại dữ liệu và nơi chứa, không ghi giá trị thật. Áp dụng cho mọi báo cáo, log test, ảnh chụp, phiếu giao việc.
- Đã sửa: che thành `<email cá nhân của Chủ dự án — đã che, xem git config>` trước khi push.
