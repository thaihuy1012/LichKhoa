# Báo cáo M8 — Báo thức đúng ngày (SPEC v1.9, D-034)

## Phiếu đã xong
| Phiếu | Thợ | Commit | Ghi chú |
|---|---|---|---|
| T-8.1 lõi `dayAlarmWindow` + `DAY_ALARM_MIN_TIME` + `AppState.dayAlarmShortcutName` + action | Gemini vòng 1 | cbbeaa6 | unit `dayalarm.test.ts` 7 ca, pass 2 TZ |
| T-8.2 nút `rem-dayalarm` + 3 chú thích + `rem-status` 5 trạng thái + ô `shortcut-dayalarm-name` | Gemini vòng 1 | d9fc850 | prop bắt buộc; 3 form mỗi form 1 dòng |
| T-8.3 HUONG-DAN §E.3 + thẻ Hướng dẫn 8 | Gemini TRỐNG → tho-sonnet | d36dad8 | lỗi làn Gemini lần 1 |
| T-8.END E2E `m8-dayalarm.spec.ts` 7 kịch bản | Gemini vòng 3/3 | cd57c71 | vòng 2 flaky webkit (xem dưới) |

Trước M8 (bảo trì sau thử tay M7, cùng khoảng diff): B-009, B-010, B-012, B-013, B-014 (hướng dẫn `ThemLoiNhac` iOS 18), SC-003 đã đóng. M7 đã đóng bằng thử tay A–F, B′ trên iPhone (`docs/bao-cao/M7.md`).

## Diff
`git diff --stat M7-ok..HEAD` (bỏ docs/gemini-out, docs/tasks, docs/test-log): 25 file, +1226/−98. Mã M8: `src/export/reminder.ts`, `src/core/model.ts`, `src/ui/store.ts`, `src/ui/components/ReminderDialog.tsx`, `src/ui/screens/{Preview,Guide}.tsx`, 3 form events, i18n, `styles.css`. `reminderWindow`, `reminderText`, `shortcutUrl` không đổi (kiem-thu xác nhận).

## Test tổng
`npm run check`: unit 290 pass (×2 TZ), e2e 102 pass / 14 skip (skip có từ trước) · `m8-dayalarm.spec.ts --repeat-each=5`: 10/10 (5 chromium + 5 webkit) · JS gzip 39,7 KB < 150 KB. Log: `docs/test-log/T-8.END-v3.txt`, `T-8.END-build.txt`.

## Sự cố trong milestone
Không có sự cố S1/S2 trong M8. (SC-003 — lời nhắc iPhone — đã đóng TRƯỚC khi mở M8; nguyên nhân ở cấu hình Phím tắt, không ở mã.)

## Quyết định mới
Không có quyết định thiết kế mới. Chủ dự án chọn làm M8 trước B-011 (B-011: việc có hạn ngày + giờ, sửa được; bỏ "nhắc 2 lần") — chờ Kiến trúc sư sau M8.

## Điểm Quản lý còn băn khoăn
1. **`ReminderDialog` giữ state `at` giữa các lần mở** — `useEffect([open])` đặt lại mặc định sau lần vẽ đầu → thoáng hiện giờ cũ; test phải bọc `setRemAt` bằng `toPass`. Đã ghi S4. Có nên sửa ngay (useLayoutEffect/key) trước khi đóng M8?
2. Khóa i18n `reminder.statusReminderOnly` còn trong json nhưng không dùng (S4 dọn).
3. §E.3 chưa kiểm được trên máy thật: "Find Reminders" lọc `Due Date is Today`, "Create Alarm" nhận Due Date làm Time, và Due Date của "Add New Reminder" có mang giờ — chỉ thử tay G1–G5 mới biết (SPEC §8.11f đã có đường dự phòng).
4. Hướng dẫn §E.3 viết theo SPEC, chưa theo ảnh chụp thật — bài học SC-003 cho thấy giao diện iOS 18 lệch mô tả; dự kiến phải chỉnh theo ảnh khi Chủ dự án thử G1.
