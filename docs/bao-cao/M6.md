# Báo cáo M6 — Cử chỉ danh sách Việc cần làm (SPEC v1.7, D-028) + bảo trì sau M5
Quản lý lập 2026-09-15 · HEAD `e8cfc2e` (cập nhật sau T-6.3) · mốc so sánh tag `M5-ok` (a173446). Diff: `docs/bao-cao/M6.diff` (src/tests/scripts, 25 file, +1558/−64).

## Phiếu từ sau M5-ok (chưa qua Kiến trúc sư)
| Phiếu | Thợ | Lần thử | Commit | Tóm tắt |
|---|---|---|---|---|
| B-003 | sonnet | 0/3 | 44d4858 | D-026: `createdAt` (LocalEvent/Occurrence, Google `created`), `dedupOccurrences` trong `collectRenderData` (chỉ hình nền) |
| T-5.4 | sonnet | 0/3 | 3b16a76 | D-025 a–d: hour12 2 hậu tố khi khác buổi; `charW(font)` mono 0.62; `truncateByFactor`; bỏ `todoDueText`; test khóa layout-week bỏ hằng lề/cột (đã khai) |
| B-004 | gemini | 0/3 | bcb4c15 | `parseFragment` nhánh lỗi phải khớp `state` |
| B-005 | sonnet | 1/3 | 3ef433a | D-027: lọc trùng mọi nguồn (local–local, Google–Google); đổi 1 case khóa B-003 (đã khai) |
| T-6.1 | sonnet | 0/3 | 34f3dd4 | `Todo.archived`; `archiveTodo`/`restoreTodo`/`reorderTodo`; `moveTodo`/`sameTodoGroup` bỏ việc lưu trữ; collect lọc |
| T-6.2 | sonnet | 2/3 | 5ca26cd | TodosTab: vuốt trái Lưu trữ/Xóa + Hoàn tác (toast có nút), phần "Đã lưu trữ", nhấn giữ 450 ms kéo sắp xếp; `touchmove {passive:false}` cho iOS; NGOÀI phạm vi `src/ui/components/Toast.tsx` (+nút `toast-undo`, chấp nhận) |
| T-6.END | sonnet | 0/3 | 4910ebd | `m6-todo-gestures.spec.ts` luồng chính |
| B-006 | gemini | 0/3 | e8cfc2e | `createStore` ghi ngay khi `pagehide` / `visibilitychange=hidden` |
| T-6.3 | sonnet | 0/3 | (commit sau báo cáo) | Sửa theo soát chéo Gemini: Hoàn tác sau Lưu trữ (trước gọi nhầm `restoreTodo` → không tác dụng); vuốt hàng đang mở không giật; dọn `suppressClick` sau cử chỉ; `touchmove` chặn sớm khi ngang; `moveTodo`/`reorderTodo` ép `order` tăng ngặt khi dữ liệu cũ trùng |

## Test tổng (kiem-thu B-006)
(sau T-6.3) tsc sạch · unit 252 × 2 TZ (21 file) · build OK · e2e 88 pass / 6 skip (6 skip có từ trước M5, không đổi). `todo-gestures` repeat-each=5 20/20; `m6-todo-gestures` repeat-each=3 6/6; `persist-flush` repeat-each=5 20/20.

## Test khóa đã đổi (khai báo)
- `tests/unit/layout-week.test.ts` (T-5.4 d): 2 test bỏ hằng lề/cột, suy từ op; bỏ 1 assertion so hằng `chipTimeSize`.
- `tests/unit/collect.test.ts` (B-005): case "hai local trùng → giữ cả hai" đổi thành "giữ 1, mục đứng sau" theo D-027.

## Sự cố
Không có SC. BAI-HOC +1 (cử chỉ iOS: `touchmove {passive:false}`; E2E reload phải chờ IndexedDB).

## Quyết định mới
D-025 (duyệt M5), D-026 (lọc trùng local/Google; bỏ "bấm đúp mở Google Calendar" → OUT), D-027 (lọc trùng mọi nguồn), D-028 (cử chỉ Việc cần làm).

## Quản lý còn băn khoăn
1. Cử chỉ vuốt/kéo mới test bằng chuột (Playwright) — chưa thử trên iPhone thật (Chủ dự án sẽ thử sau deploy). Rủi ro: ngưỡng 16 px / 450 ms / 40% có hợp tay không; `pointercancel` giữa chừng.
2. TodosTab.tsx +406 dòng; bộ nhận cử chỉ đã là hook `useRowGesture` nhưng vẫn cùng file (soát chéo #8 đính chính).
3. Hàng việc vẫn giữ nút ▲▼ 🗑 cũ (test khóa dùng) → hàng khá chật ở 428 px; bỏ sau nếu Chủ dự án muốn.
4. ~~`reorderTodo` với `order` trùng~~ → đã sửa T-6.3 (#5).
5. `dedupOccurrences` khóa theo tên chuẩn hóa `toLowerCase()` — tên khác dấu (vd "Hop" vs "Họp") không coi là trùng (đúng D-026, chỉ ghi nhận).
6. Toast: lần xóa thứ 2 trong 5 s thay toast trước → mất nút Hoàn tác của lần 1.

## Soát chéo (`docs/bao-cao/M6-soat-cheo.md`, Gemini Pro — lượt 1 tho-gemini tự soát thay Gemini đã bị bỏ, BAI-HOC)
Quản lý kiểm chứng: #1 (thật, S3 — không phải S1), #2, #3, #5 thật → sửa T-6.3; #4 làm luôn (rẻ); #6 toast đè, #7 listener `createStore` không gỡ (store 1 lần/phiên), #10 unit `createStore` → S4 tồn đọng; #8 đính chính báo cáo; #9 đã thêm test.
