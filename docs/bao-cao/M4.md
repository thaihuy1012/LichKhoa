# Báo cáo M4 — Ảnh nền & tùy biến, đặt hình nền một chạm, hướng dẫn, hoàn thiện iPhone
Quản lý · 2026-09-14 · HEAD `fd13eb7` · từ tag `M3-ok`

## 1. Phiếu
| Phiếu | Nội dung | Lần thử | Commit |
|---|---|---|---|
| T-4.1 | Ảnh nền: coverFit/fitDownscale/blurDownsampleRatio thuần, loadPhoto, drawBackground (không `ctx.filter`); fixture 4000×3000 | 0 | a00063e |
| T-4.5 | `scripts/size.mjs [dir]` + `.github/workflows/pages.yml` (workflow_dispatch + push main; KHÔNG push) | 0 | a1e8024 |
| T-4.3 | `copyPng` (giữ user-activation), `openShortcut` + `__lastNav`, nút Sao chép / Đặt hình nền, tên Shortcut | 0 | 5b1790f |
| T-4.2 | Màn Thiết kế (nền ảnh/màu/gradient, mờ, tối, màu, font, vị trí, cỡ chữ, boxAlpha 0.35 khi ảnh — D-012); đồng bộ ngay sau kết nối (D-015); nút Đồng bộ accent; ảnh nền lưu ArrayBuffer (D-017) | 0 (chạm trần lượt 1 lần) | 2e6517a |
| T-4.4 | Màn Hướng dẫn + `docs/HUONG-DAN.md` (cài PWA, Client ID, Shortcut, hosting, hằng ngày, giới hạn, bài thử iPhone) | 0 | 008eefd |
| T-4.6 | Hoàn thiện UI Thiết kế (rãnh range, nhãn giá trị, hex, swatch viền) + sửa lỗi subscribe sau mount ở Design **và Sync** (lặp T-1.END) | 1 | dfb8ab9 |
| T-4.END | `m4-design`, `m4-export`, `m4-offline` | 0 | fd13eb7 |
| T-4.7 | Sửa soát chéo #1/#4/#6/#10: pages.yml `contents: read`, Xóa dữ liệu xóa cả ảnh nền, `ImageBitmap.close()` + revoke, nền dưới PNG trong suốt; ảnh nguồn png/webp/gif → PNG, còn lại JPEG 0.9 | 1 | (commit sau báo cáo) |
| T-4.0 | **Thử máy thật iPhone 13 Pro Max** (D-015/D-016) — Chủ dự án | — | **CHỜ** (mục 4) |

## 2. Thay đổi mã
`git diff --stat M3-ok..HEAD`: 35 files, +2736 / −70. Diff: `docs/bao-cao/M4.diff` (không gồm ảnh fixture).

## 3. Kết quả test tổng (kiem-thu — `docs/test-log/M4-cuoi.txt`)
- `npm run check`: tsc sạch · Vitest 195 × 2 TZ · build OK · Playwright pass (chromium + webkit iPhone 13 Pro Max).
- `m4-* --repeat-each=3`: 12 pass / 6 skip. Sau T-4.7 (`docs/test-log/T-4.7.txt`): Vitest 197 × 2 TZ; toàn bộ e2e `--repeat-each=2`: **116 pass / 12 skip / 0 fail**. Test M1–M3: `git diff M3-ok` rỗng.
- `node scripts/size.mjs`: **36,40 KB** gzip (< 150 KB).
- Mọi `test.skip` (webkit): `share.spec.ts:13,34,46`, `m4-export.spec.ts:15` — Playwright-WebKit không cấp `clipboard-write`; `m4-offline.spec.ts:36`, `m3-google.spec.ts:262` — lỗi nội bộ WebKit khi offline+reload (D-013); webkit vẫn khẳng định `caches.match` có app shell.

## 4. Thử máy thật (T-4.0) — CÓ KẾT QUẢ 2026-09-14
`dist` @ `f4f6098`; Chủ dự án host bằng **URL GitHub** (không dùng Netlify). iPhone 13 Pro Max, PWA Màn hình chính.
- (A) ✔ Không văng sang Safari: bấm Kết nối → đăng nhập → quay về đúng cửa sổ app, hiện "Đã kết nối" + công tắc chọn các lịch Google.
- (B) ✔ Sau đăng nhập: "Đã kết nối" + danh sách lịch.
- (C) ✔ Hình nền: chọn 1 lịch → Đồng bộ ngay → Xem trước bố cục Tháng có chấm trên ngày có sự kiện; Agenda hiện đúng sự kiện.
  ✘ **Tab Sự kiện không hiện sự kiện Google** (không chấm, không danh sách). Nguyên nhân (Quản lý xác minh): `src/ui/screens/events/EventsTab.tsx:59` chỉ `expandOccurrences(state.events, …)` — không trộn `state.google.cache`. SPEC không yêu cầu → không phải regression; là thiếu UX → phiếu mới (mục 8).
- (Hình nền) ✔ Đặt hình nền qua Shortcut: chữ KHÔNG bị đồng hồ/widget/nút che (vùng an toàn 0.30/0.14 đạt). Đặt ảnh nền làm hình nền khóa: không bị đè. → Soát chéo #3 (ClipboardItem trên Safari iOS) **hoạt động**.
  ⚠ Đổi bố cục rồi bấm "Đặt hình nền" lần nữa → Shortcut báo `The operation couldn't be completed. (com.apple.extensionKit.errorDomain error 2.)`; bấm lại lần nữa thì đặt được. Lỗi nằm ở hành động "Đặt hình nền" của iOS Shortcuts (không phải mã app) → ghi vào HUONG-DAN mục khắc phục.
- (D) ✔ Chế độ máy bay → đóng hẳn app → mở icon: app mở, còn sự kiện Google đã lưu.
- (E) ✔ Sau > 1 giờ, "Kết nối lại" → kết nối ngay, không phải đăng nhập Google lại.
- **Yêu cầu mới của Chủ dự án**: bố cục Tháng chỉ thấy ngày, không thấy nội dung sự kiện → muốn thêm danh sách sự kiện + việc cần làm ngay dưới lưới tháng ("nhiều quá thì có thanh cuộn"). Hình nền là ảnh tĩnh → không cuộn được; cần chốt cách cắt bớt → đổi SPEC (IN-2) → hỏi Chủ dự án.

### Thử máy thật lần 2 (dist @5f8401c, sau T-4.8/T-4.9) — 2026-09-14
- Tháng + danh sách: ✔ dễ đọc, số dòng đủ, chữ và số âm lịch ổn; đặt hình nền: danh sách không bị che.
- Tab Sự kiện: ✔ ngày có sự kiện Google có chấm và hiện trong danh sách.
- ✘ Ghim ghi chú nhưng hình nền không hiện → nút "Ghim lên hình nền" không bật `showNote` (mặc định tắt) → T-4.10.
- Chủ dự án đồng ý nối repo GitHub + workflow tự deploy (D-019).

## 4b. Soát chéo (Gemini Pro — `docs/bao-cao/M4-soat-cheo.md`; lần 1 làn trả rỗng, thử lại 1 lần được)
- CÓ THẬT → T-4.7: #1 pages.yml thiếu `contents: read`; #4 Xóa dữ liệu không xóa ảnh nền; #6 không `ImageBitmap.close()`; #10 PNG trong suốt không có nền dưới.
- Quản lý sửa tài liệu: #8 HUONG-DAN thêm bước bật nhận đầu vào cho Shortcut.
- BÁC: #2 (Quản lý build `VITE_BASE=/lichkhoa/` → manifest `scope`/`start_url` `/lichkhoa/`, `sw.js` NavigationRoute index.html — plugin tự lấy base); #7 (skip webkit có điều kiện, D-013); #9 (giữ ảnh khi đổi nền là cố ý).
- Chờ máy thật: #3 ClipboardItem Blob vs Promise trên Safari iOS (bước "Đặt hình nền" của T-4.0). #5 → S4.

## 5. Sự cố
Không có S1/S2.

## 6. Quyết định mới
D-016 (Chủ dự án dời thử máy thật về cuối M4), D-017 (ảnh nền lưu ArrayBuffer trong IndexedDB).

## 7. Điểm Quản lý còn băn khoăn
1. **T-4.0 chưa có kết quả** — mọi rủi ro iOS (OAuth standalone, SW offline trên WebKit, clipboard + `shortcuts://` thật, vùng an toàn 0.30/0.14 so với đồng hồ/widget) chỉ máy thật xác nhận được.
2. **Lỗi subscribe sau mount lặp lại lần 2** (T-1.END → T-4.6, cả Sync.tsx): đã sửa 2 màn + ghi BAI-HOC; đề xuất hook chung `useStoreState` nếu có phiếu refactor (không làm trong M4).
3. **Workflow GitHub Pages** chỉ chạy trên nhánh `main`; repo đang `master`, chưa có remote. Deploy chính thức cần Chủ dự án quyết (push).
4. Tồn đọng S4 còn lại: `parseFragment` giải mã 2 lần (đã sửa T-3.4 — xóa khỏi danh sách), input color webkit hiện hex 2 lần, `App.tsx` "Đang tải…" (đã làm T-3.3).
