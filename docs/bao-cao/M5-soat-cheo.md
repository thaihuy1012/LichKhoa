KẾT QUẢ: XONG
SỐ VẤN ĐỀ: S1=0 S2=2 S3=2 S4=2

1. S2 — src/ui/sync.ts:16 — Máy đang có cache cũ (timeMin = today-1), nâng lên v1.5 mở layout Tuần -> các ngày đầu tuần bị trống sự kiện Google cho đến lần sync tiếp theo — Cache cũ vẫn hợp lệ về thời gian nên không tự force sync dù khoảng đồng bộ yêu cầu đã mở rộng ra today-7 — Thêm điều kiện `cache.timeMin > syncRange(today).timeMin` vào `shouldAutoSync` để kích hoạt đồng bộ.
2. S2 — src/core/collect.ts:448 — Tạo sự kiện Local trùng tên và giờ với sự kiện Google Calendar -> Lưới hiển thị 2 chip trùng lặp — Gộp `[...localOcc, ...googleOcc]` rồi sort mà không có bước deduplicate các sự kiện — Dùng `.filter()` loại bỏ `googleOcc` trùng title và time với `localOcc` trước khi gộp.
3. S3 — src/render/layout/week.ts:691 — Tạo to-do không có ngày đến hạn, chọn bố cục Tuần -> to-do bị mất tích, không hiển thị ở đâu — Hàm `dayItems` chỉ lấy to-do `overdue` (`due < date`) và `due === date`, bỏ qua hoàn toàn to-do không có `due` — Gom bổ sung các to-do không có `due` vào danh sách của cột hôm nay.
4. S3 — src/render/layout/week.ts:671 — Sự kiện từ 11:00 AM đến 01:00 PM -> label chip hiện "11:00 - 01:00 PM" (mất chữ AM ở giờ đầu) — Code luôn dùng regex xóa hậu tố ở giờ bắt đầu để tiết kiệm chỗ, dễ gây hiểu nhầm nếu sự kiện vắt qua buổi trưa — Chỉ cắt bỏ hậu tố ở `start12` nếu hậu tố đó giống hệt hậu tố của `end` (cùng AM/PM).
5. S4 — src/render/layout/week.ts:950 — Mở ứng dụng trên trình duyệt iOS / Safari — Ký tự Unicode ◷ và ☐ có nguy cơ bị OS tự động render thành emoji màu mặc định, phá vỡ thiết kế màu text/pastel — Thêm Variation Selector-15 (`\uFE0E`) ngay sau ký tự để ép thiết bị render dưới dạng văn bản đơn sắc.
6. S4 — tests/e2e/m5-week.spec.ts:139 — Đọc code test hàm `containsX` — Test chỉ kiểm tra tọa độ `x` của chip, không kiểm tra tọa độ `y` nên chip có bị vẽ đè lên nhau hoặc rơi ra khỏi dải (tràn layout) thì test vẫn pass — Bổ sung assert tọa độ `y` để đảm bảo chip nằm gọn trong khoảng `chipsTop` và không đè lên list danh sách.
