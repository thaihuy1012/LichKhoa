# Báo cáo M3 — Google Calendar (OAuth thuần client, chỉ đọc) + trộn dữ liệu
Quản lý · 2026-09-14 · HEAD `58182e2` · từ tag `M2-ok` (gồm 2 phiếu nối tiếp M2 theo D-012)

## 1. Phiếu đã xong
| Phiếu | Nội dung | Lần thử | Commit |
|---|---|---|---|
| T-2.15 | (nối tiếp M2) trần agenda 12 dòng sự kiện; normalizeState mảng + ≤1 ghim; ics weekdays DTSTART + gập dòng UTF-8 | 0 | fc566c3 |
| T-2.16 | (nối tiếp M2) `scripts/mau-anh.cjs`, `chup.cjs`, `cat-anh.cjs` | 0 | 86563d2 |
| T-3.1 | `google/oauth.ts`: buildAuthUrl, parseFragment, newState, save/get/clearToken (storage tiêm được) | 0 | 3cc3ec0 |
| T-3.2 | `google/calendar.ts`: fetchCalendars, fetchEvents (≤2 trang, Bearer), normalize thuần, AuthError, all-day nhiều ngày tách ngày | 0 | 6e77145 |
| T-3.3 | Màn Đồng bộ + `ui/sync.ts` (handleAuthRedirect, autoSyncIfNeeded, performSync) gọi ở cấp App; `consumeState`; "Đang tải…" i18n | 1 | 86940fa |
| T-3.END | `m3-google.spec.ts` + fixture ngày động | 0 | 58182e2 |
Lần thử 1 của T-3.3: review mã (trước kiểm thử) phát hiện xử lý hash OAuth + tự đồng bộ nằm trong `Sync.tsx` → chỉ chạy khi mở tab Đồng bộ (redirect về Preview không nhận token; TH1 không tự đồng bộ). Đã chuyển lên App.

## 2. Thay đổi mã
`git diff --stat M2-ok..HEAD`: 34 files, +2166 / −55. Diff: `docs/bao-cao/M3.diff`.

## 3. Kết quả test tổng (kiem-thu — `docs/test-log/T-3.3.txt`, `webkit-offline.txt`)
- `npm run check`: tsc sạch · Vitest 177 × 2 TZ · build OK · Playwright 37 pass / 1 skip (webkit, bước offline+reload — D-013).
- `m3-google --repeat-each=3`: 15 pass / 3 skip (webkit). Toàn bộ e2e `--repeat-each=2` (sau T-3.3): 64/64.
- `Select-String -Path src -Pattern client_secret -Recurse`: rỗng. Test M1–M2: không đổi (chỉ thêm).

## 4. Đối chiếu tiêu chí M3 (SPEC §6)
- `oauth.test.ts`: response_type=token, scope calendar.readonly, state, redirect_uri, prompt; parseFragment đúng/sai state/error/rỗng ✔.
- `google-normalize.test.ts`: all-day → allDay đúng ngày; dateTime offset khác → đúng giờ địa phương ở cả 2 TZ; cancelled bỏ; nhiều lịch gộp + sắp xếp; 401 → AuthError ✔.
- E2E T-3.END: 302 (HTML chuyển hướng, D-013) về `#access_token…&state=<query>` → app tự mở tab Đồng bộ → chọn lịch → Đồng bộ → agenda có sự kiện fixture → offline + reload vẫn hiện (chromium; webkit skip D-013) → 401 → "Kết nối lại" ✔; tự đồng bộ khi cache > 30′ ✔.

## 5. Sự cố
Không có S1/S2.

## 6. Quyết định mới
D-012 (duyệt M2 — áp dụng: 2 phiếu nối tiếp, CLAUDE.md cấm thợ đổi cây git), D-013 (mock OAuth bằng trang HTML; webkit offline+reload skip, bắt buộc thử lại ở T-4.END + thử tay iPhone chế độ máy bay).

## 7. Điểm Quản lý còn băn khoăn
1. **Webkit offline (D-013)**: kiem-thu tái hiện "WebKit encountered an internal error" khi reload offline trang thật; `data:` URL không lỗi; chromium không lỗi. Chưa loại trừ SW của app không phục vụ navigation trên WebKit → rủi ro thật cho iPhone. Có nên thêm kiểm tra khác ngay (vd. `caches.match` trong page) hay để T-4.END?
2. **Token trong localStorage** (SPEC §5 cho phép): XSS đọc được token `calendar.readonly` 1 giờ. App không nhúng nội dung ngoài; chấp nhận?
3. **`fetchEvents` tự gọi calendarList** để lấy màu lịch (giữ chữ ký SPEC) → mỗi lần đồng bộ +1 request. Có nên đổi chữ ký (truyền màu) ở M4?
4. **Nút chính xám** ở màn Đồng bộ (S4 → T-4.2).
5. **Chủ dự án chưa tạo Client ID thật** → M3 chỉ kiểm bằng mock; thử thật dồn về M4 (HUONG-DAN + thử tay iPhone). Ảnh mẫu đặt thử làm hình nền: vẫn chưa có phản hồi.
