# Báo cáo bàn giao M1 — Khung PWA + dựng hình nền bố cục Tháng
Quản lý lập ngày 2026-09-13. SPEC hiện hành: v1.1 (v1.1 chỉ thêm Âm lịch vào M2, không đổi M1).

## 1. Phiếu đã xong (6/6)
| Phiếu | Commit | Model | Lần thử | Ghi chú |
|---|---|---|---|---|
| T-1.1 Khung dự án + bộ chạy test | 9c68ee8 | sonnet | 0/3 | Chậm do SC-001 (không có shell). vite-plugin-pwa 1.3 + Vitest 5 chạy tốt với Vite 8 (rủi ro 5 không xảy ra). |
| T-1.2 Model + mặc định + monthGrid | 6fd9230 | sonnet | 0/3 | Chủ dự án dừng thợ giữa chừng; thợ mới giữ phần dở, viết test. |
| T-1.3 Thiết bị + layoutMonth | 9547de1 | sonnet | 1/3 | Review trả: số ngày lệch tâm vòng hôm nay (baseline), chấm sự kiện chìm, nhãn preset sai. |
| T-1.4 paint + wallpaper + db + store | 86c28fc | sonnet | 0/3 | Chạy song song T-1.3; `DrawOp` bị khai 2 nơi → Quản lý gộp về `layout/common.ts` (D-005). |
| T-1.5 Màn Preview + Lưu ảnh | 1ab1a48 | sonnet | 1/3 | Review trả: ô tùy chỉnh nhận 0 (canvas 0×0, lỗi không bắt), revoke URL sớm hủy download WebKit, render cũ ghi đè ảnh mới. |
| T-1.END Kiểm thử tích hợp M1 | f295576 | sonnet | 1/3 | Thợ né "flake" webkit trong test; Quản lý xác định là bug race (subscribe store sau paint) → tái hiện 1/10 fail → sửa 1 dòng `Preview.tsx` → 0 fail qua repeat-each=5. |

## 2. Thay đổi mã (952e46b → f295576, trừ docs và lockfile)
38 file, +1416 / −8. Chính: `src/core/{model,calendar}.ts`, `src/render/{devices,paint,wallpaper}.ts`, `src/render/layout/{common,month}.ts`, `src/storage/db.ts`, `src/ui/{App.tsx,store.ts,styles.css,screens/Preview.tsx}`, `src/export/share.ts`; test: `tests/unit/{smoke,calendar,layout-month,store}.test.ts`, `tests/e2e/{smoke,m1-render}.spec.ts`.
Dependency đúng SPEC mục 4: runtime `preact`, `idb-keyval`; dev thêm `@types/node`, `cross-env` (D-004). Phiên bản ghim cứng: Vite 8.3.0, TS 7.0.2, Vitest 5.0.0, Playwright 1.63.0 (SPEC ghi 1.62).

## 3. Kết quả test tổng (`npm run check`, kiem-thu độc lập, log `docs/test-log/T-1.END-kiem-thu.log`)
- `tsc --noEmit` sạch · Vitest 22/22 × 2 TZ (Asia/Ho_Chi_Minh, UTC) · build OK (`dist/manifest.webmanifest`, `dist/sw.js`) · Playwright 4/4 (smoke + m1-render × chromium, webkit).
- `m1-render.spec.ts --repeat-each=5`: 10/10 pass, 0 fail.

## 4. Đối chiếu tiêu chí M1 trong SPEC mục 6
- [x] `calendar.test.ts` + `layout-month.test.ts`: monthGrid 2/2026 (T2) & 3/2026 (CN), 6×7, ngoài tháng null; layoutMonth ≥ 28 op text ngày, tô hôm nay bằng accentColor, mọi op trong vùng an toàn (3 position × 2 thiết bị).
- [x] T-1.END chromium + webkit: chọn 1179×2556 → preview 1179×2556 → "Lưu ảnh" tải `.png` > 10 KB → reload giữ thiết bị (IndexedDB).
- [x] `<link rel="manifest">` + `navigator.serviceWorker.ready` (smoke.spec).

## 5. Sự cố đã đóng trong M1
- SC-001 (S1): subagent không có shell (`tools: Bash` trong khi phiên chỉ có `PowerShell`) → thêm `PowerShell` vào tools (D-003) + mở lại Claude Code. Đóng 2026-09-13.

## 6. Quyết định mới (DECISIONS.md)
- D-003 thêm PowerShell vào tools · D-004 phiên bản thực tế · D-005 `DrawOp` khai ở `layout/common.ts` · D-006 (Chủ dự án) thêm Âm lịch → SPEC v1.1, phiếu T-2.8; tài liệu tham khảo `F:\LICH_NEN` gắn vào phiếu M2/M4 (`docs/tham-khao-LICH_NEN.md`).

## 7. Điểm Quản lý còn băn khoăn
1. Chất lượng hình ảnh chưa được ai nhìn bằng mắt: test chỉ kiểm hình học (vùng an toàn, kích thước, số op). Căn chữ dùng hệ số ước lượng `size·0.35` cho baseline. Đề nghị Kiến trúc sư xem có cần một bước "Chủ dự án xem ảnh mẫu" trước M2 không.
2. `boxAlpha` mặc định 1 (hộp đen đặc) sẽ che ảnh nền ở M4 — đã ghi S4, xử lý ở T-4.2.
3. `store.load` thay nguyên state: state cũ trong IndexedDB sẽ thiếu trường mới (`showLunar` ở T-2.8). Chưa có bước migrate/merge mặc định — dự kiến xử lý trong T-2.6/T-2.8; xin ý kiến có cần phiếu riêng.
4. Thợ sonnet có xu hướng "né" hành vi lạ trong E2E (đã ghi BAI-HOC) — M2 có nhiều E2E hơn, tôi sẽ nhắc trong mọi phiếu T-n.END.
5. Test PNG > 10 KB đang pass nhờ chữ khử răng cưa; nền đen nén rất tốt — ngưỡng có thể mong manh nếu đổi mặc định.
