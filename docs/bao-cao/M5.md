# Báo cáo M5 — Bố cục Tuần giống app Inks (SPEC v1.5, D-024)
Quản lý lập 2026-09-15 · HEAD `1bff8e2` · mốc so sánh `6fec4b5` (hết đợt bảo trì D-023; tag gần nhất `M4-ok`).

## Phiếu đã xong (4/4)
| Phiếu | Thợ | Lần thử | Commit | Tóm tắt |
|---|---|---|---|---|
| T-5.1 | sonnet | 0/3 | fa08074 | `Occurrence.endTime` (local = time + durationMin, Google = end.dateTime cùng ngày); `collectRenderData` expand từ min(đầu tháng, today−6); `syncRange` [today−7, +60]; `layout: 'week'` |
| T-5.2 | sonnet | 1/3 | fea2a4e | `render/layout/week.ts` `layoutWeek`; common +`weekDates`, +`pastel`; `buildOps` 'week'; i18n `week.todayEmpty`, `week.more`; mau-anh +week. Lượt 1 bị trả: chip luôn rút về giờ bắt đầu ở 1284 px |
| T-5.3 | gemini (flash) | 0/3 | 9d915df | nút `layout-week` "Tuần"/"Week" trong Preview; `week.spec.ts` |
| T-5.END | sonnet | 1/3 | 1bff8e2 | `m5-week.spec.ts` luồng chính; lượt 1 bị trả: test chép cứng hằng hình học của week.ts |

## Thay đổi (`git diff --stat 6fec4b5 HEAD -- src tests scripts`)
19 file, +1139 / −14. Lớn nhất: `src/render/layout/week.ts` +415, `tests/unit/layout-week.test.ts` +231, `tests/e2e/m5-week.spec.ts` +163, `tests/e2e/week.spec.ts` +135.

## Test tổng (`npm run check`, kiem-thu T-5.END)
tsc sạch · unit 228 × 2 TZ (21 file) · build OK · e2e 70 pass / 6 skip (6 skip có từ trước M5, không đổi) · `m5-week.spec.ts --repeat-each=3` 6/6.

## Test khóa đã đổi (khai báo, phiếu cho phép)
- `tests/unit/store.test.ts` test `syncRange`: tên + `timeMin '2026-03-03'` (SPEC v1.5 đổi khoảng đồng bộ).
- `tests/unit/google-normalize.test.ts` test "dateTime offset khác…": thêm `endTime` vào object mong đợi.

## Sự cố trong M5
Không có (không mở SC nào). BAI-HOC +2 dòng (kiem-thu chép số test cũ; ước lượng độ rộng chữ ở cột hẹp).

## Quyết định mới
D-024 — SPEC v1.5 bố cục Tuần (5 điểm Chủ dự án chốt: to-do có hạn/quá hạn dồn hôm nay; > 4 mục → 3 + "+N"; có danh sách hôm nay; chip pastel theo màu lịch, to-do vàng; âm lịch nhỏ ở đầu cột). Không cần Kiến trúc sư khi mở phạm vi (tiền lệ D-011, D-018).

## Bằng chứng xem bằng mắt
- Mẫu Inks: `docs/tham-khao/inks-tuan.PNG`.
- Hình nền Tuần 1284×2778: `docs/test-log/T-5.2/week-note0-2.png` (không note), `week-note1-15.png` (có note).

## Quản lý còn băn khoăn
1. Glyph "◷" (danh sách hôm nay) và "☐" (chip to-do) mới xem trên chromium Windows; chưa xác nhận font iOS Safari vẽ đúng (Chủ dự án xem trên máy thật sau deploy).
2. Chip chữ ≈ 20–24 px ở 1284 px (≈ 7–8 pt) — nhỏ như Inks; chưa biết Chủ dự án thấy dễ đọc không.
3. Khoảng giờ trong chip dựa ước lượng độ rộng 0.55×size (không đo font thật) — tên dài bị "…" sớm/muộn tùy font.
4. Cache Google cũ (đồng bộ trước v1.5 với timeMin today−1) thiếu các ngày đầu tuần đến lần đồng bộ kế tiếp — tự hết sau 1 lần đồng bộ, không migrate.
5. `week.ts` 415 dòng một file — chấp nhận (cùng cỡ month.ts?), chưa tách.
