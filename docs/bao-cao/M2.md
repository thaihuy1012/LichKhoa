# Báo cáo M2 — Sự kiện, to-do, ghi chú; Agenda / To-do / Note; sao lưu; i18n (+ SPEC v1.3)
Quản lý · 2026-09-14 · HEAD `5d6d5b7` · từ tag `M1-ok`

## 1. Phiếu đã xong (14/14)
| Phiếu | Nội dung | Lần thử | Commit |
|---|---|---|---|
| T-2.1 | recurrence `expandOccurrences` | 0 | bd411e1 |
| T-2.4 | i18n `t()` + vi/en.json | 0 | 4f92649 |
| T-2.5 | `eventToIcs` + backup (qua normalizeState) | 0 | 09f25f0 |
| T-2.2 | `groupAgenda` + `collectRenderData` | 0 | 07717a8 |
| T-2.3 | layoutAgenda / Todo / Note (+ chia vùng note) | 1 | 4a1be63 |
| T-2.8 | Âm lịch (SPEC v1.1, D-006) | 1 | c805953 |
| T-2.6 | `buildOps` + 11 action store + nhãn tháng/thứ i18n | 1 | 17d739e |
| T-2.7 | Tab Sự kiện: lịch nhỏ + sheet sự kiện, to-do (UI theo LICH_NEN, D-010) | 1 | 1d412a5 |
| T-2.10 | Lõi v1.3: `weekdays`, `alarmMin`→VALARM, `Todo.due`, `Note[]`, bỏ `noteText` (D-011) | 0 | 24936c7 |
| T-2.11 | Hình nền v1.3: nhãn hạn to-do, tiêu đề ghi chú ghim | 0 | 192d1e3 |
| T-2.12 | Tab Sự kiện v1.3: T2–T6, nhắc trước, hạn, danh sách ghi chú | 0 | 7a4a5d2 |
| T-2.9 | Preview: bố cục, cài đặt chung, xuất/nhập/xóa JSON | 1 | b7010d5 |
| T-2.13 | Agenda: bỏ tiêu đề ngày mồ côi khi cắt 12 dòng | 0 | 30e926f |
| T-2.END | E2E `m2-events` (2 test) + `m2-i18n` (1 test) | 1 | 5d6d5b7 |
Không phiếu nào nâng bậc model; mọi "Lần thử 1" là review bằng mắt (ảnh 1284×2778 / chụp webkit 13 Pro Max), không phải test fail.

## 2. Thay đổi mã
`git diff --stat M1-ok..HEAD`: 62 files changed, +5805 / −135 (gồm docs). Diff đầy đủ: `docs/bao-cao/M2.diff`.

## 3. Kết quả test tổng (kiem-thu, cổng cuối — `docs/test-log/M2-cuoi.txt`)
- `npm run check`: tsc sạch · Vitest 129 pass × 2 TZ (Asia/Ho_Chi_Minh, UTC) · build OK · Playwright 26 pass (chromium + webkit `iPhone 13 Pro Max`).
- `m2-*.spec.ts --repeat-each=3`: 18/18. Test M1 (`smoke`, `m1-render`, `iphone13pm`): `git diff M1-ok` rỗng.

## 4. Đối chiếu tiêu chí M2 (SPEC v1.3 §6)
- recurrence (daily/weekly/monthly 31/yearly 29-02/until/weekdays) · ics (VCALENDAR, VEVENT, DTSTART, RRULE, BYDAY, VALARM, CRLF) · layout (agenda thứ tự, to-do ≤ 12 + tick + nhãn hạn, note bọc dòng + tiêu đề) · i18n vi=en · lunar 6 mốc · collect (thứ tự to-do, ghi chú ghim) · model (noteText → Note pinned): unit ✔.
- T-2.END: lặp tuần → Agenda → hash PNG đổi → reload → xuất JSON → xóa → nhập → sự kiện + ghi chú ghim + hạn + nhắc trước trở lại ✔; `en` → nhãn tab; 12h → AM/PM; tắt âm lịch → không op "Âm lịch"; T2–T6 so đúng tập ngày trên Agenda; "Quá hạn"; ghim ghi chú 2 → chỉ tiêu đề ghi chú 2 ✔.

## 5. Sự cố
Không có S1/S2 trong M2. (SC-001 thuộc M1, đã đóng.)

## 6. Quyết định mới
- D-009 bật làn Gemini (agy 1.2.2 cần `--add-dir`; `accept-edits` cho làn ghi; kết quả rỗng = lỗi).
- D-010 tách T-2.7/T-2.9, UI theo hướng LICH_NEN; cài đặt chung đặt dưới nút "Lưu ảnh" (SPEC giữ 5 tab).
- D-011 SPEC v1.3 (Chủ dự án): lặp T2–T6, nhắc giờ = VALARM trong .ics, hạn to-do (có hạn xếp trước), nhiều ghi chú ghim 1. Hợp đồng §5 đổi: `Repeat`+`weekdays`, `LocalEvent.alarmMin?`, `Todo.due?`, `Note`, `AppState.notes`, bỏ `DesignConfig.noteText`, `RenderData.noteTitle?`; vẫn `version: 1` nhờ `normalizeState`. Test khóa sửa đúng 3 chỗ `noteText/setNote` (collect, store, wallpaper) — kiem-thu xác nhận.

## 7. Điểm Quản lý còn băn khoăn (nhờ Kiến trúc sư xem)
1. **SPEC v1.3 do Quản lý tự viết** (sau khi Chủ dự án chọn 3 phương án) — chưa qua Kiến trúc sư. Nhờ soát §3 IN-10, §5, §6 M2 (v1.3).
2. **Trần 12 dòng agenda tính cả tiêu đề ngày** → 15 sự kiện/7 ngày chỉ hiện ~7 sự kiện + "+8". Đúng chữ SPEC §9 nhưng có thể quá ít cho màn khóa 1284×2778 (còn nhiều chỗ trống). Có nên tính trần theo chiều cao khả dụng thay vì số dòng?
3. **Nền mặc định đen + `boxAlpha=1`** (S4 từ T-1.2) — hình nền hiện là chữ trắng trên đen; M4 mới có ảnh. Chưa có phản hồi của Chủ dự án về ảnh mẫu đặt thử (đè đồng hồ/widget?).
4. **Công cụ dựng ảnh mẫu có dữ liệu + chụp webkit** đang nằm ở scratchpad phiên (`mau-v13.cjs`, `chup-*.cjs`) — quy tắc BAI-HOC yêu cầu dùng mỗi phiếu layout/UI; nên đưa vào `scripts/` (phiếu công cụ nhỏ).
5. **Song song**: một thợ dùng `git stash` khi thợ khác đang sửa (không mất gì; đã ghi BAI-HOC + quy tắc prompt).
6. Tồn đọng S4: `boxAlpha` mặc định; `normalizeState` chỉ kiểm width/height; `eventToIcs` chưa gập dòng 75 byte; ô `todo-due` trống không có nhãn.
