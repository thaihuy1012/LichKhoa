# Báo cáo M9 — Việc có giờ hạn + sửa hạn sau khi tạo (SPEC v1.10, D-035)

## Phiếu đã xong
| Phiếu | Thợ | Commit | Ghi chú |
|---|---|---|---|
| T-9.1 `Todo.dueTime` + `normalizeState` + `cmpTodo` + `sameTodoGroup` + `addTodo`/`setTodoDue` + `defaultReminderAt` | Gemini vòng 1 | 98805da | unit `todo-time.test.ts` 7, pass 2 TZ |
| T-9.2 `todoDueLabel(…, dueTime?, hour12?)` + Tháng/To-do/Tuần | Gemini vòng 1 (báo cáo cụt) | 726e6c8 | unit `layout-todo-time.test.ts` 11 |
| T-9.3 tab Việc: `todo-due-time`, bộ sửa hạn, i18n, `color-scheme`; S4 M8 #1 (ReminderDialog wrapper + Inner), #2 (xóa khóa thừa) | Gemini vòng 1 (báo cáo cụt) | 86a3534 | test khóa todo-touch/gestures/m7/m8 pass |
| T-9.END E2E `m9-todo-time.spec.ts` 7 kịch bản | **tho-gemini tự viết** (vi phạm làn, BAI-HOC) | 1b53059 | kiem-thu độc lập PASS |

## Diff
`git diff --stat 32ebe26..1b53059 -- src tests`: 14 file src/test. `due` giữ kiểu ISODate; JSON sao lưu `version: 1`.

## Test tổng
`npm run check`: unit 308 pass ×2 TZ; e2e 104 pass / 14 skip (có từ trước) · `m9-todo-time.spec.ts --repeat-each=5`: 10/10 (5 chromium + 5 webkit) · JS gzip 46,1 KB < 150 KB. Log: `docs/test-log/T-9.*.txt`.

## Sự cố
Không có S1/S2 trong M9.

## Quyết định
D-035 (Kiến trúc sư + Chủ dự án duyệt 2026-09-19): giờ tùy chọn; "Quá hạn" theo NGÀY; deploy M8 + M9 gộp một lần.

## Điểm Quản lý còn băn khoăn
1. T-9.END do người liên lạc (Haiku) viết, không qua Gemini — đã qua kiem-thu + Quản lý đọc assert; đề nghị Kiến trúc sư nhìn thêm chất lượng test.
2. Kịch bản (6) dùng `toHaveValue` (tự đợi) — với lỗi cũ S4-#1 test có thể vẫn pass sau vài lần thử lại; kiểm S4-#1 thật sự nằm ở cấu trúc mã (không còn `useEffect([open])`).
3. Giao diện ô giờ + bộ sửa hạn chưa được xem bằng mắt ở 428 pt — dựa vào thử tay H1–H2 sau deploy.
4. `cmpTodo` nay coi `due: ''` như không có hạn (trước so chuỗi) — thay đổi hành vi nhỏ, test khóa vẫn pass.

## Thử tay iPhone (Chủ dự án, 2026-09-21) — M9 ĐÓNG
- H1 ĐẠT · H2 ĐẠT · H3 ĐẠT · H4 ĐẠT (bản deploy `index-BLKpwWNR.js`).
- Góp ý mới (ngoài SPEC, không phải lỗi M9): việc ĐÃ TẠO không mở được "Nhắc trên iPhone" — nút chỉ có ở hàng thêm việc (IN-11/IN-13 theo thiết kế). Chờ Chủ dự án quyết định có mở rộng phạm vi.
