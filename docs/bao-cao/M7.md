# Báo cáo milestone M7 — Nhắc trên iPhone qua Phím tắt (SPEC v1.8, D-031)

Ngày: 2026-09-19 · Quản lý gửi Kiến trúc sư · Phạm vi diff: `M6-ok..HEAD` (kèm cả B-007/B-008 sửa sau tag M6-ok — không thuộc M7).

## 1. Phiếu đã xong (5/5 + 1 phiếu vá + 1 phiếu hạ tầng)
| Phiếu | Nội dung | Giao cho | Kết quả |
|---|---|---|---|
| T-7.1 | Lõi thuần: `export/reminder.ts`, `shortcutUrl`/`openShortcut(name, text?)`, 2 tên Phím tắt trong `AppState` + 2 action | tho-sonnet | PASS vòng 1 |
| T-7.2 | `ReminderDialog` + gắn form Sự kiện + 2 ô tên ở tab Xem trước | **Gemini** (vòng 1) | PASS, nhưng i18n/CSS bị script hoàn tác do phiếu ghi sai đường dẫn |
| T-7.2b | Vá: 12 khóa i18n VI/EN + 6 class CSS | tho-sonnet | PASS vòng 1 |
| T-7.3 | Gắn hộp thoại vào form Việc + form Ghi chú | **Gemini** (vòng 1) | PASS vòng 1 |
| T-7.4 | `HUONG-DAN.md` §E cài 2 Phím tắt + thẻ Guide 7 | **Gemini** (vòng 1 → 2) | Vòng 1 sót 3 lỗi hướng dẫn (Quản lý review bắt), vòng 2 PASS |
| T-7.END | E2E 6 kịch bản `m7-reminder.spec.ts` | **Gemini** (vòng 1) | PASS vòng 1 |
| T-G.1 | Hạ tầng làn Gemini: lệnh `song` + `gia-han`, hạn giờ mềm | tho-sonnet | PASS sau 1 vòng sửa theo review |

## 2. Kết quả test (Quản lý tự chạy, không chỉ tin báo cáo thợ)
- `npx playwright test tests/e2e/m7-reminder.spec.ts` → **2 passed** (chromium 2.3 s, webkit 6.0 s) — 6 kịch bản trong một luồng.
- `npm run check` → **99 passed / 13 skipped / 0 fail**; unit 274 × 2 múi giờ; tsc + build sạch.
- 13 skip là skip có sẵn từ M6 (webkit không có CDP touch), không do M7.

## 3. Đối chiếu tiêu chí nghiệm thu SPEC §6 M7
- [x] Unit `reminder.test.ts` + `share-url.test.ts` + bổ sung `model/backup/store.test.ts` — kiểm thử độc lập xác nhận đủ ca SPEC liệt kê, giá trị mong đợi khớp SPEC.
- [x] E2E 6 kịch bản, payload so sánh sau `decodeURIComponent`, thời điểm tính từ `new Date()` (không cắm cứng ngày).
- [x] `HUONG-DAN.md` §E: `Select-String` khớp 21 dòng (yêu cầu ≥ 5); có mục "Bật Nhạy cảm về thời gian" và "Nếu không chạy" (4 tình huống).
- [x] `guide.reminderTitle`/`guide.reminderBody` có ở cả `vi.json` và `en.json`.
- [x] Không sửa/skip test khóa M1–M6 (kiểm thử độc lập xác nhận từng phiếu).
- [ ] **Thử tay trên iPhone (A–F): CHƯA LÀM** — cần Chủ dự án cài 2 Phím tắt rồi thử. Đây là điều kiện đóng M7 theo SPEC, không chặn nghiệm thu bằng lệnh.

## 4. Quyết định mới
- **D-031** — SPEC v1.8: IN-11 "Nhắc trên iPhone qua Phím tắt" (Kiến trúc sư soạn, Chủ dự án duyệt).
- **D-032** — Cổng bảo mật trước deploy + xử lý phần đã push trước khi có quy định: kiểm toán hồi tố 143 commit → sạch (không secret/PII thật); Chủ dự án chọn không viết lại lịch sử, chuyển sang email ẩn GitHub, giữ kho công khai; siết `.gitignore`; mở phiếu T-G.2 viết `scripts/kiem-bao-mat.sh`.
- Quy định mới trong `CLAUDE.md`: (a) ưu tiên giao Gemini để tiết kiệm token Claude (LAN-GEMINI §4b); (b) theo dõi lượt Gemini mỗi 10 phút + gia hạn khi còn đang làm (§5b); (c) Cổng bảo mật trước deploy.

## 5. Sự cố trong milestone
Không có sự cố S1/S2. Một sự việc mức S4 đáng ghi: phiếu T-7.2 ghi sai đường dẫn (`src/i18n/` thay vì `src/core/i18n/`) → script làn Gemini hoàn tác phần i18n/CSS, `npm run check` vẫn pass nên suýt lọt. Đã ghi `BAI-HOC.md` và từ đó mọi phiếu đều xác minh đường dẫn bằng `git ls-files` trước khi giao.

## 6. Điểm Quản lý còn băn khoăn (xin ý kiến Kiến trúc sư)
1. **Không có cách kiểm bằng lệnh rằng hướng dẫn Phím tắt đúng thực tế iOS.** Tiêu chí chỉ kiểm được "có chuỗi đó trong file". Vòng 1 Gemini viết 3 chỗ sai (Split Text hướng dẫn ngược; bước Share Sheet thừa; phím tắt Lời nhắc lấy dòng 3 không tồn tại khi payload chỉ 2 dòng → lỗi ở form Sự kiện/Việc) — chỉ phát hiện được bằng đọc. Có nên coi loại phiếu "tài liệu thao tác thiết bị" là loại bắt buộc Quản lý review tay, ghi vào quy chế?
2. **E2E gộp 6 kịch bản vào một `test()`** (dùng `test.step`): kịch bản 1 hỏng thì 5 cái sau không chạy, khó khoanh vùng. Có cần tách thành 6 `test()` độc lập không, hay giữ vì đúng tinh thần "một luồng chính"?
3. **Báo thức chỉ có giờ, không có ngày** (giới hạn iOS, SPEC §8.10a): app chặn "Thêm báo thức" ngoài 24 h. Nhưng nếu Chủ dự án đặt báo thức lúc 23:50 cho 00:30 hôm sau thì vẫn trong 24 h và đúng; còn đặt cho "9 h sáng mai" lúc 10 h sáng nay (23 h nữa) cũng lọt cửa sổ và cũng đúng. Có trường hợp nào trong 24 h mà báo thức vẫn kêu sai ngày không — Kiến trúc sư soát giúp lập luận này.
4. **`reminderText` thay xuống dòng trong ghi chú bằng ` · `** (để payload giữ đúng 3 dòng). Ghi chú dài nhiều dòng sẽ thành một dòng dính nhau trong app Lời nhắc. Chấp nhận được hay cần cách khác?
