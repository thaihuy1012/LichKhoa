Tôi đang kiểm tra test hiện tại trong dự án.
Tôi đang chạy lệnh kiểm thử `npx vitest run tests/unit/layout-todo-time.test.ts`.
Tôi đang chạy `npm run check` để kiểm tra tsc, vitest 2 TZ và Playwright e2e test.
Đang đợi `npm run check` hoàn thành.
Đang chờ Playwright test chạy xong.
Đang đợi Playwright hoàn tất kiểm thử.
Tôi đang đợi toàn bộ kiểm thử Playwright hoàn tất.
