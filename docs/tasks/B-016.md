THỢ CODE — làn code — vòng 1
# Phiếu B-016: Toast: thao tác mới không làm mất Hoàn tác của thao tác trước

## Việc cần làm
- Mục tiêu: đóng S4 M6 (a) (soát chéo M6 #6). Quyết định D-036: gộp Hoàn tác.
- Phạm vi file: `src/ui/screens/Events.tsx`, `tests/e2e/b016-toast-undo.spec.ts` (mới).
- Giao diện có sẵn: `showToast(msg, action?)` trong Events.tsx (~L29–40) ghi đè `toastAction`; nút `toast-undo` gọi `toastAction.onClick()`. TodosTab gọi `showToast` khi xóa (`restoreTodo`) / lưu trữ (`archiveTodo archived:false`) — KHÔNG sửa TodosTab.
- Yêu cầu: giữ danh sách hành động Hoàn tác đang chờ (ref). `showToast` có action trong lúc toast có action cũ còn hiện → nối thêm; bấm `toast-undo` chạy MỌI hành động đang chờ theo thứ tự ngược (mới nhất trước) rồi xóa danh sách. Toast hết giờ (5 s tính từ lần gọi cuối) hoặc `showToast` không có action → xóa danh sách. Chữ toast = thông điệp mới nhất.
- Tiêu chí nghiệm thu: 
  - [ ] e2e (chromium + webkit): thêm 3 việc, xóa việc 1 rồi xóa việc 2 liên tiếp (trong 5 s) → bấm `toast-undo` → cả 2 việc trở lại đúng thứ tự
  - [ ] xóa 1 việc → bấm Hoàn tác → trở lại (hành vi cũ giữ)
  - [ ] lưu trữ + xóa liên tiếp → Hoàn tác khôi phục cả hai
  - [ ] `m6-todo-gestures.spec.ts`, `todo-gestures.spec.ts` pass nguyên vẹn
  - [ ] `npm run check` pass

## D-036 — Dọn tồn đọng 2026-09-22: gộp Hoàn tác; khôi phục giữ chỗ cũ
Toast đè Hoàn tác (M6 a): gộp — thao tác có Hoàn tác liên tiếp trong lúc toast còn hiện thì một nút Hoàn tác khôi phục tất cả (mới nhất trước). Lý do: không mất thao tác, không cần hàng đợi toast/chữ mới.

## Đọc trước (theo thứ tự, đọc đủ rồi mới sửa)
AGENTS.md, docs/SPEC.md, src/ui/screens/Events.tsx, src/ui/screens/events/TodosTab.tsx (dòng 575–600 handleArchive/handleDelete, không sửa), tests/e2e/m6-todo-gestures.spec.ts (mẫu cách thêm việc, vuốt xóa/lưu trữ, không sửa), src/ui/components/Toast.tsx (không sửa)

## File được phép tạo/sửa (mỗi dòng một đường dẫn; kết thúc bằng / nghĩa là cả thư mục)
- src/ui/screens/Events.tsx
- tests/e2e/b016-toast-undo.spec.ts

## Lệnh kiểm thử (chạy trước khi kết thúc; test fail thì sửa rồi chạy lại, tối đa 3 lần)
`npx tsc --noEmit` và `npx playwright test tests/e2e/b016-toast-undo.spec.ts` (chỉ file mới này; có thể thêm `--project=chromium` nếu cần nhanh)

## Cấm
Sửa file ngoài danh sách trên. Sửa, xóa, skip hay nới lỏng test có sẵn. Chạy lệnh git thay đổi trạng thái. Cài gói mới. KHÔNG chạy `npm run check` hay toàn bộ Playwright — kiem-thu sẽ chạy sau.

## BÁO CÁO CUỐI (in đúng khung này ở cuối, bằng tiếng Việt, không thêm lời dẫn)
KẾT QUẢ: XONG | XONG-CÓ-LƯU-Ý | KHÔNG XONG
TEST: <lệnh> — PASS/FAIL — <số test đạt/tổng>
FILE ĐÃ TẠO/SỬA: <danh sách>
QUYẾT ĐỊNH TỰ CHỌN: <điểm phiếu không nói rõ và cách đã chọn; ghi "không" nếu không có>
LƯU Ý/CÂU HỎI: <tối đa 5 dòng; ghi "không" nếu không có>
