THỢ CODE — làn code — vòng 2

# Phiếu B-016: Toast: thao tác mới không làm mất Hoàn tác của thao tác trước

## Việc cần làm

### B-016 — Toast: thao tác mới không làm mất Hoàn tác của thao tác trước
- Mục tiêu: đóng S4 M6 (a) (soát chéo M6 #6). Quyết định D-036: gộp Hoàn tác.
- Phạm vi file: `src/ui/screens/Events.tsx`, `tests/e2e/b016-toast-undo.spec.ts` (mới).
- Giao diện có sẵn: `showToast(msg, action?)` trong Events.tsx (~L29–40) ghi đè `toastAction`; nút `toast-undo` gọi `toastAction.onClick()`. TodosTab gọi `showToast` khi xóa (`restoreTodo`) / lưu trữ (`archiveTodo archived:false`) — KHÔNG sửa TodosTab.
- Yêu cầu: giữ danh sách hành động Hoàn tác đang chờ (ref). `showToast` có action trong lúc toast có action cũ còn hiện → nối thêm; bấm `toast-undo` chạy MỌI hành động đang chờ theo thứ tự ngược (mới nhất trước) rồi xóa danh sách. Toast hết giờ (5 s tính từ lần gọi cuối) hoặc `showToast` không có action → xóa danh sách. Chữ toast = thông điệp mới nhất. **Siết (D-036, sau vòng 1):** chỉ nối thêm khi từ lần `showToast` có action trước tới lần này, store nhận đúng 1 `dispatch` (đếm bằng `store.subscribe` trong Events.tsx, nhớ hủy đăng ký khi unmount); nhiều hơn → bỏ danh sách cũ, chỉ giữ action mới. Bản thân hành động Hoàn tác dispatch cũng không được làm lệch đếm (danh sách đã xóa sau khi bấm).
- Tiêu chí nghiệm thu: [ ] e2e (chromium + webkit): thêm 3 việc, xóa việc 1 rồi xóa việc 2 liên tiếp (trong 5 s) → bấm `toast-undo` → cả 2 việc trở lại đúng thứ tự · [ ] xóa 1 việc → bấm Hoàn tác → trở lại (hành vi cũ giữ) · [ ] lưu trữ + xóa liên tiếp → Hoàn tác khôi phục cả hai · [ ] thêm kịch bản: xóa X → thêm việc Y → xóa Z → Hoàn tác → chỉ Z trở lại, X không · [ ] `m6-todo-gestures.spec.ts`, `todo-gestures.spec.ts` pass nguyên vẹn · [ ] `npm run check` pass.
- Lệnh kiểm tra: `npx tsc --noEmit` ; `npx playwright test tests/e2e/b016-toast-undo.spec.ts tests/e2e/m6-todo-gestures.spec.ts` (kem kiểm tra test khóa).
- Model: gemini (2 file) · Lần thử: 2/3 · Vòng Gemini: 2/3 · Trạng thái: DOING

### D-036 — Dọn tồn đọng 2026-09-22: gộp Hoàn tác; khôi phục giữ chỗ cũ (2026-09-22 · người quyết: Quản lý)
- Chủ dự án: "xử lý hết tồn đọng"; mục "Nhắc trên iPhone cho việc cũ" (ngoài SPEC) → Chủ dự án chọn giữ thủ công, đóng.
- Toast đè Hoàn tác (M6 a): gộp — thao tác có Hoàn tác liên tiếp trong lúc toast còn hiện thì một nút Hoàn tác khôi phục tất cả (mới nhất trước). Lý do: không mất thao tác, không cần hàng đợi toast/chữ mới.
  - Siết (B-016 vòng 1 REGRESSION `m6-todo-gestures.spec.ts:172`): chỉ gộp khi giữa hai lần `showToast` có hành động, store nhận ĐÚNG 1 `dispatch` (chính thao tác mới). Có thay đổi khác xen giữa (thêm/sửa/kéo việc…) → xóa danh sách chờ, chỉ giữ Hoàn tác mới nhất. Lý do: Hoàn tác không được khôi phục thứ người dùng đã "bỏ qua" bằng thao tác khác.
- Khôi phục trùng `order` (M6 d): dời các việc `order >=` lên 1, việc khôi phục giữ đúng chỗ cũ. `sameTodoGroup` khớp `cmpTodo` (`due: ''` = không hạn).

## Kết quả kiểm thử lần trước

```
Running 60 tests using 4 workers
  ✘   1 [chromium] › tests\e2e\m6-todo-gestures.spec.ts:89:1 › T-6.END luồng chính...
    Error: expect(locator).toHaveCount(expected) failed
    Locator:  getByTestId('todo-item')
    Expected: 3
    Received: 4
    Timeout:  5000ms
    
    170 |   await expect(rows).toHaveCount(2);
    171 |   await page.getByTestId('toast-undo').click();
    > 172 |   await expect(rows).toHaveCount(3);
    
Kịch bản lỗi (m6-todo-gestures.spec.ts:89–180): vuốt xóa "ViecLuuTru" (toast Hoàn tác hiện) → thêm A,B,C → vuốt xóa A → bấm toast-undo → cả A lẫn ViecLuuTru trở lại (4 hàng thay vì 3, SAI).
Nguyên nhân: quy tắc gộp L49 chỉ kiểm danh sách cũ có dữ liệu không, không kiểm có thay đổi nào xen giữa → gộp thao tác xóa A lên danh sách từ lúc xóa ViecLuuTru.
Siết cần thiết: đếm `dispatch` của store; chỉ gộp nếu store nhận đúng 1 `dispatch` (là thao tác xóa/lưu trữ vừa rồi); > 1 → bỏ danh sách cũ.
```

## Đọc trước (theo thứ tự, đọc đủ rồi mới sửa)
- AGENTS.md, CLAUDE.md, docs/LAN-GEMINI.md
- docs/SPEC.md (mục 5, 6, 8–10)
- src/ui/screens/Events.tsx (hiện tại, bản v1 đã cất ở docs/tasks/B-016-v1.Events.tsx.txt)
- src/ui/store.ts (type Store, tìm subscribe method)
- tests/e2e/b016-toast-undo.spec.ts (bản v1 đã cất ở docs/tasks/B-016-v1.spec.ts.txt)
- tests/e2e/m6-todo-gestures.spec.ts (dòng 89–180, để hiểu kịch bản lỗi)

## File được phép tạo/sửa (mỗi dòng một đường dẫn)
- src/ui/screens/Events.tsx
- tests/e2e/b016-toast-undo.spec.ts

## Lệnh kiểm thử (chạy trước khi kết thúc; test fail thì sửa rồi chạy lại, tối đa 3 lần)
```
npx tsc --noEmit
npx playwright test tests/e2e/b016-toast-undo.spec.ts tests/e2e/m6-todo-gestures.spec.ts
```

## Cấm
Sửa file ngoài danh sách trên. Sửa, xóa, skip hay nới lỏng test có sẵn. Chạy lệnh git thay đổi trạng thái. Cài gói mới.

## BÁO CÁO CUỐI (in đúng khung này ở cuối)
KẾT QUẢ: XONG | XONG-CÓ-LƯU-Ý | KHÔNG XONG
TEST: <lệnh> — PASS/FAIL — <số test đạt/tổng>
FILE ĐÃ TẠO/SỬA: <danh sách>
QUYẾT ĐỊNH TỰ CHỌN: <điểm phiếu không nói rõ và cách đã chọn; ghi "không" nếu không có>
LƯU Ý/CÂU HỎI: <tối đa 5 dòng; ghi "không" nếu không có>
