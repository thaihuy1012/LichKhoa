THỢ CODE — làn code — vòng 1
# Phiếu T-8.END: E2E `tests/e2e/m8-dayalarm.spec.ts`

## Việc cần làm
### Phạm vi file
- `tests/e2e/m8-dayalarm.spec.ts` (mới). Không sửa src.

### Tiêu chí nghiệm thu (7 kịch bản E2E M8)
Từ SPEC.md mục 6 (E2E `m8-dayalarm.spec.ts`), chromium + webkit, `page.clock.setFixedTime(new Date('2026-10-05T10:00:00'))`; `npx playwright test tests/e2e/m8-dayalarm.spec.ts --repeat-each=3` pass; `npm run check` pass; `node scripts/size.mjs` JS gzip < 150 KB.

Chi tiết 7 kịch bản:
- **(1)** sự kiện có giờ ngày mai 07:30 → mở sửa → `rem-open` → `rem-dayalarm` bật → bấm → `__lastNav` có `name=ThemBaoThucNgay&input=text&text=`, payload = `'6 Oct 2026 07:30\n<tên>'`; Sheet đóng; danh sách vẫn 1 sự kiện; IndexedDB đã chứa sự kiện ngay sau khi bấm (như M7 (3)).

- **(2)** mở lại → `rem-at` = `2026-10-05T23:59` → `rem-dayalarm` disabled, `rem-alarm` và `rem-reminder` enabled, `rem-status` chứa "Hôm nay".

- **(3)** `rem-at` = `2026-10-06T00:10` → `rem-dayalarm` disabled, `rem-status` chứa "00:30"; `2026-10-06T00:30` → enabled.

- **(4)** `rem-at` = `2026-10-08T10:00` → `rem-alarm` disabled, `rem-dayalarm` và `rem-reminder` enabled; `2026-10-05T09:00` → cả ba disabled.

- **(5)** nhập việc "Mua sữa" (chưa bấm Thêm) → `rem-open` → `rem-at` = `2026-10-06T08:00` → `rem-dayalarm` → việc trong danh sách, ô nhập rỗng, `__lastNav` `name=ThemBaoThucNgay`, payload dòng 2 = `Mua sữa`.

- **(6)** hộp có đủ `rem-hint-alarm` / `rem-hint-dayalarm` / `rem-hint-reminder`, không rỗng; đổi ngôn ngữ `en` → `rem-dayalarm` có chữ "Alarm on that day".

- **(7)** tab Xem trước `shortcut-dayalarm-name` = `Hen Bao Thuc` → reload giữ → `__lastNav` chứa `name=Hen%20Bao%20Thuc`.

### Test khóa
- [ ] Test khóa M7 pass nguyên vẹn: `m7-reminder.spec.ts` 6 kịch bản (kịch bản (4) chỉ kiểm 2 nút cũ nên vẫn đúng), `reminder.test.ts`, `share-url.test.ts`, `model.test.ts`, `backup.test.ts`, `store.test.ts`.

### Đầu vào có sẵn
- File tham khảo: `tests/e2e/m7-reminder.spec.ts` (mẫu: ?test=1, window.__lastNav, giải mã text=, đọc IndexedDB)
- `src/ui/components/ReminderDialog.tsx` (testid)
- `src/ui/screens/Preview.tsx` (shortcut-dayalarm-name)
- Model `dayAlarmWindow` (M8 code đã làm ở T-8.1, T-8.2, T-8.3) — kiểm được qua unit test `dayalarm.test.ts`

## Đọc trước (theo thứ tự)
1. AGENTS.md
2. docs/SPEC.md (toàn bộ mục 6–7)
3. tests/e2e/m7-reminder.spec.ts (mẫu E2E M7)
4. src/ui/components/ReminderDialog.tsx (kiểm testid: rem-open, rem-dayalarm, rem-alarm, rem-reminder, rem-status, rem-hint-alarm, rem-hint-dayalarm, rem-hint-reminder)
5. src/ui/screens/Preview.tsx (shortcut-dayalarm-name input)
6. docs/LAN-GEMINI.md (quy tắc làn Gemini)

## File được phép tạo/sửa
- tests/e2e/m8-dayalarm.spec.ts (file mới)

## Lệnh kiểm thử
```
npx playwright test tests/e2e/m8-dayalarm.spec.ts --repeat-each=3
npm run check
npm run build && node scripts/size.mjs
```

## Cấm
- Sửa file ngoài danh sách trên (không sửa src/)
- Sửa, xóa, skip hay nới lỏng test có sẵn
- Chạy lệnh git thay đổi trạng thái cây làm việc
- Cài gói mới
- Hỏi lại (không có ai trả lời): chọn phương án an toàn nhất và ghi vào báo cáo

## Ghi chú thêm
- Sử dụng `page.clock.setFixedTime(new Date('2026-10-05T10:00:00'))` để cố định giờ trang (bắt buộc cho kịch bản dựa vào "hôm nay"/"ngày mai")
- Cách tách dòng: dùng `\n` trong payload (giống M7)
- IndexedDB: dùng cách của `m7-reminder.spec.ts` để kiểm
- Test được gọi từ `npm run check` nên phải pass trên cả chromium + webkit
