THỢ CODE — làn code — vòng 1

# Phiếu T-9.3: UI tab Việc (ô giờ, bộ sửa hạn) + i18n + 2 S4 M8

## Việc cần làm

### T-9.3 — UI tab Việc (ô giờ, bộ sửa hạn) + i18n + 2 S4 M8

**Phạm vi file:** `src/ui/screens/events/TodosTab.tsx`, `src/ui/components/ReminderDialog.tsx`, `src/core/i18n/vi.json`, `src/core/i18n/en.json`, `src/ui/styles.css`.

**Yêu cầu + tiêu chí:** SPEC §6 M9 "Nội dung" (TodosTab, i18n, `color-scheme: dark`, S4-#1 wrapper + Inner, S4-#2 xóa khóa) và gạch tiêu chí 4 (test khóa pass nguyên vẹn). Vùng chạm ≥ 44 px, bề ngang 428 pt.

### Nội dung SPEC M9 (nguyên văn dòng 246, 250):

`Todo.dueTime?: 'HH:mm'` (tùy chọn, chỉ khi có `due`; KHÔNG đổi kiểu `due`) + `normalizeState` lọc; `cmpTodo` cùng ngày → có giờ trước, giờ ↑, rồi `order`; `sameTodoGroup` thêm điều kiện cùng `dueTime`; action `addTodo.dueTime?`, `setTodoDue { due, dueTime? }`; `todoDueLabel(due, today, lang, dueTime?, hour12?)` — nhãn kèm giờ, "Quá hạn" theo ngày không kèm giờ; 3 bố cục Tháng-danh-sách / To-do / Tuần (chip Tuần dòng 1 `☐ 14:00`); `defaultReminderAt` việc = `due` + (`dueTime` ?? `08:00`); **`TodosTab`: ô `todo-due-time` ở hàng thêm việc (chỉ hiện khi `todo-due` có giá trị), bộ sửa hạn `todo-due-edit` + `todo-due-time-edit` + `todo-due-done`, xóa ngày → mất giờ, hộp nhắc từ hàng nhập truyền `dueTime`; i18n `events.todoDueTime` ("Giờ" / "Time"), `events.todoDueDone` ("Xong" / "Done"); ô giờ `color-scheme: dark` như B-010; JSON sao lưu vẫn `version: 1`. Gộp 2 S4 của M8: (S4-#1) `ReminderDialog` tách wrapper `if (!open) return null` + `ReminderDialogInner` mount mới mỗi lần mở (`useState(() => defaultReminderAt(…))`, bỏ `useEffect([open])`), props và `data-testid` không đổi; (S4-#2) xóa khóa `reminder.statusReminderOnly` khỏi `vi.json` + `en.json`.**

### Tiêu chí nghiệm thu (nguyên văn SPEC §6 M9, gạch 3 và 4):

  - [ ] E2E `m9-todo-time.spec.ts` — **T-9.END** (chromium + webkit, `?test=1`, `page.clock.setFixedTime(new Date('2026-10-05T10:00:00'))` trước khi mở app): (1) tab Việc: `todo-due-time` không hiện khi `todo-due` rỗng; nhập "Nộp báo cáo", `todo-due` = `2026-10-06` → `todo-due-time` hiện → `14:00` → `todo-add` → `todo-due-label` = `6/10 14:00`; IndexedDB (`keyval-store`) có việc `dueTime: '14:00'`; ô nhập rỗng, ô giờ ẩn lại. (2) thêm "Họp" `2026-10-06` `09:00` và "Đọc" `2026-10-06` không giờ → thứ tự `todo-item`: Họp · Nộp báo cáo · Đọc. (3) chạm `todo-due-label` của "Đọc" → `todo-due-edit` và `todo-due-time-edit` hiện → giờ `08:00` → `todo-due-done` → nhãn `6/10 08:00`, "Đọc" lên đầu. (4) sửa "Đọc": `todo-due-edit` = `''` → `todo-due-done` → nhãn `+ Hạn`, IndexedDB không còn `due`/`dueTime` của việc đó, "Đọc" xuống nhóm không hạn. (5) thêm "Gọi điện" `2026-10-05` `16:00` → bố cục To-do: `__lastOps` chứa `Hôm nay 16:00`; bố cục Tuần: chứa `☐ 16:00`. (6) nhập "Mua sữa" `2026-10-07` `06:45` (chưa Thêm) → `rem-open` → `rem-at` = `2026-10-07T06:45` ngay lần đọc đầu (không `toPass`) → `rem-none` đóng, ô nhập còn nguyên; đổi ngày nhập sang `2026-10-08` → `rem-open` lần 2 → `rem-at` = `2026-10-08T06:45` ngay lần đọc đầu (S4-#1). (7) reload → 4 việc còn đúng nhãn; đổi ngôn ngữ `en` → nhãn "Gọi điện" = `Today 16:00`.
  - [ ] Test khóa pass nguyên vẹn (không sửa/skip): `events.spec.ts`, `m2-events.spec.ts`, `m5-week.spec.ts`, `m6-todo-gestures.spec.ts`, `todo-touch.spec.ts`, `todo-gestures.spec.ts`, `settings.spec.ts`, `m7-reminder.spec.ts`, `m8-dayalarm.spec.ts`; `i18n.test.ts` pass sau khi xóa `reminder.statusReminderOnly` ở cả 2 file.
  - [ ] `npm run build` + `node scripts/size.mjs`: JS gzip < 150 KB; `npm run check` pass trên chromium + webkit.

## Đọc trước

AGENTS.md, docs/SPEC.md, src/ui/store.ts (addTodo.dueTime, setTodoDue{due,dueTime}), src/render/layout/common.ts (todoDueLabel chữ ký mới), src/export/reminder.ts; e2e todo-touch/todo-gestures/m7/m8 (không sửa, giữ testid).

## File được phép tạo/sửa

- src/ui/screens/events/TodosTab.tsx
- src/ui/components/ReminderDialog.tsx
- src/core/i18n/vi.json
- src/core/i18n/en.json
- src/ui/styles.css

## Lệnh kiểm thử

`npx playwright test tests/e2e/todo-touch.spec.ts tests/e2e/todo-gestures.spec.ts tests/e2e/m7-reminder.spec.ts tests/e2e/m8-dayalarm.spec.ts` ; `npm run check`

## Kết quả kiểm thử lần trước

(vòng 1 — không có)

## Cấm

Sửa file ngoài danh sách trên. Sửa, xóa, skip hay nới lỏng test có sẵn. Chạy lệnh git thay đổi trạng thái. Cài gói mới. Hỏi lại — không có ai trả lời: điểm nào phiếu không nói rõ thì chọn phương án an toàn nhất và ghi vào báo cáo.
